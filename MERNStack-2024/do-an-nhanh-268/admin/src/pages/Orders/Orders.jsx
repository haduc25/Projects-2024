import { useState } from 'react';
import './Orders.css';

const Orders = () => {
    const [orders, setOrders] = useState([]);

    return (
        <div>
            <p>Orders</p>
        </div>
    );
};

export default Orders;
